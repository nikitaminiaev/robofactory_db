[ ] Операция копирования ролей а может и модуля в целом очень важна - ведь изобретательство подразумевает постоянную генерацию и отсев  множества вариантов. А давайте так , а что если сяк. Генерируя и потом серьёзно рассматривая ИИ может серьёзно работать.
Надо прояснит для себя статус этих копий. Это то что раньше я называл версией модуля чтобы подчеркнуть что не все версии модуля (почти ни одна) удостоятся какого то релиза, но все они где то должны числиться.
[ ] Гипотетическая история переходов между версиями для данного примера. В реальном софте полезно на каждом узле одним предложением описать в чем отличие от предыдущего.
    - Это наверное можно сделать через git визуализаторы истории, если в таблице с версиями будет записан хеш коммитов
    - и наверное .git файл нужен тогда на каждый модуль и ссылка в БД на него
    - Brep пишем не напрямую в db, а тоько ссылки на файлы где уже есть сами файлы .brep
	 и они лежат в api в папке с названием ресурсы которая в gitignore и кадлая в отдельной дир со свим именем
    - В каждй это дир есть свой .git и при сохранении меняется файл и делается коммпит в этот репо 
	
